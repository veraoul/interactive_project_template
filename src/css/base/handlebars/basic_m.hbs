{{!--
.sprite{
  background-image: url({{{escaped_image}}});
}
--}}
{{#sprites}}
@media (max-width: 640px){
.sprite.{{name}} {
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}}; }
}
{{/sprites}}